<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- This EditText MUST have the id @+id/inputGoalName -->
    <EditText
        android:id="@+id/inputGoalName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/add_goal_hint_name"
        android:inputType="textCapWords"
        android:autofillHints="name" />

    <!-- This EditText MUST have the id @+id/inputTargetAmount -->
    <EditText
        android:id="@+id/inputTargetAmount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:hint="@string/add_goal_hint_target"
        android:inputType="number"
        android:importantForAutofill="no" />

</LinearLayout>
